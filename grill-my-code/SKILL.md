---
name: grill-my-code
description: Run a grilling session focused on implementation details — questions the AI's just-written code, not the plan. (user-invoke only)
disable-model-invocation: true
---

Run a `/grill-implementation` session.

Use when the user wants to be grilled about code that has just been (or is being) generated — every `if`, business rule, or domain operation. For grilling about a plan or design before building, use `/grill-me` instead.
